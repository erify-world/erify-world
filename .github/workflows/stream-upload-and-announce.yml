name: Stream Upload and Announce

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install GitHub CLI
        run: sudo apt-get install -y gh

      - name: Upload files
        run: echo 'Uploading files...'

      - name: Add PR comment
        run: echo 'Adding comment to PR...'

